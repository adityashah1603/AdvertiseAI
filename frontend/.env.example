# Copy to .env.local and fill in real values (same secrets as the root
# .env - see .env.example there for what each one is / DECISIONS.md §3.4
# for the credential-blast-radius reasoning). Only used server-side, inside
# Route Handlers (lib/supabase.ts) - never exposed to the browser, never
# prefixed NEXT_PUBLIC_.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
